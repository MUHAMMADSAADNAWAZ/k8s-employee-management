apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: devops-demo
spec:
  selector:
    app: postgres
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: 5432
  type: ClusterIP
