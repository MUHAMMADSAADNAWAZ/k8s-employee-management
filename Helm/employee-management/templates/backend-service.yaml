apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: devops-demo
spec:
  selector:
    app: node
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 3001
  type: ClusterIP
