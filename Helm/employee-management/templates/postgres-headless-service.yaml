apiVersion: v1
kind: Service
metadata:
  name: postgres-headless-service
  namespace: devops-demo
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: 5432
